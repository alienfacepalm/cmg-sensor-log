import { evaluateSensorLog } from './evaluate-sensor-log';

describe('evaluateSensorLog', () => {
  it('should work', () => {
    expect(evaluateSensorLog()).toEqual('evaluate-sensor-log');
  });
});
